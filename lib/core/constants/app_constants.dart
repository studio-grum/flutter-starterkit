class AppConstants {
  AppConstants._();

  static const appName = 'Flutter Starter Kit';
  static const defaultPadding = 16.0;
  static const defaultRadius = 12.0;
  static const defaultAnimationDuration = Duration(milliseconds: 300);

  // RevenueCat entitlement IDs
  static const entitlementPremium = 'premium';

  // Route paths
  static const routeHome = '/';
  static const routeLogin = '/login';
  static const routeRegister = '/register';
  static const routePaywall = '/paywall';
  static const routeVideoPlayer = '/video-player';
  static const routeAudioPlayer = '/audio-player';
}
